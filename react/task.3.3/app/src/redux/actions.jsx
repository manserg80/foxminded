export const addPosts = (posts) => ({
    type: 'ADD_POSTS',
    payload: posts,
  });
  
  export const addTodos = (todos) => ({
    type: 'ADD_TODOS',
    payload: todos,
  });
  
  export const addUsers = (users) => ({
    type: 'ADD_USERS',
    payload: users,
  });